export const Card = ({repo,})=>{
    return (
        <div className={` card bg-white shadow-md rounded-lg p-4 m-2 `}>
            //className avec variable repo.language dans la classe
         

            <div className="card-body">
             
            </div>
        </div>
    )
}